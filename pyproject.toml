# Project metadata ------------------------------------------------------------
[project]
name = "markov-agent"
version = "1.2.0"
description = "A coding agent following SKILL.state paper principles."
requires-python = ">=3.13"
license = { text = "MIT" }
readme = "README.md"
authors = [{ name = "Nacho Llorca", email = "nllorca@proton.me" }]
dependencies = [
    "lmdk>=2.10.3",
]

# Dev / optional dependencies -------------------------------------------------
[dependency-groups]
dev = [
    "logfire",
    "complexipy",
    "pytest-cov",
    "ruff",
    "ty",
]

# Build backend ---------------------------------------------------------------
[build-system]
requires = ["uv_build>=0.11.1,<0.12.0"]
build-backend = "uv_build"

# Ruff (lint + format) --------------------------------------------------------
[tool.ruff]
fix = true
line-length = 100

[tool.ruff.lint]
select = ["D", "E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["D105", "D107"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["D"]

# Semantic release (automated versioning from conventional commits) -----------
[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
build_command = ""  # We do this using uv in the CI verify.yaml workflow
commit_message = "chore: release {version}"
commit_parser = "conventional"
tag_format = "v{version}"
changelog_file = false

[tool.semantic_release.branches.main]
match = "main"
prerelease = false

# Spelling --------------------------------------------------------------------
# [tool.codespell]
# skip = ["", ""]  # Comma separated list of regex strings to ignore files for the spelling check
# ignore-words-list = "firstword,secondword"  # Specific strings to accept
